<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".MainActivity">

    <!-- Track title and file browse button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <TextView
            android:id="@+id/tv_track_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="marquee"
            android:maxLines="1"
            android:focusable="true"
            android:marqueeRepeatLimit="marquee_forever"
            android:scrollHorizontally="true"
            android:text="@string/no_file_loaded"
            android:textColor="@color/text_primary"
            android:textSize="18sp"
            android:textStyle="bold" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_open_file"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:contentDescription="@string/open_file"
            android:text="@string/open_file"
            app:icon="@drawable/ic_folder_open"
            app:iconGravity="start"
            app:strokeColor="@color/accent" />

    </LinearLayout>

    <!-- Time display -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="4dp">

        <TextView
            android:id="@+id/tv_current_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:text="00:00"
            android:textColor="@color/text_primary"
            android:textSize="14sp" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <TextView
            android:id="@+id/tv_total_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:text="00:00"
            android:textColor="@color/text_secondary"
            android:textSize="14sp" />

    </LinearLayout>

    <!-- Loop-aware seek bar -->
    <com.musicpracticeplayer.LoopSeekBar
        android:id="@+id/seek_bar"
        android:layout_width="match_parent"
        android:layout_height="32dp"
        android:layout_marginBottom="12dp"
        android:max="1000"
        android:progress="0"
        android:thumbTint="@color/accent"
        android:progressTint="@color/accent"
        android:progressBackgroundTint="@color/text_secondary" />

    <!-- Main transport controls -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="16dp">

        <!-- Return to start -->
        <ImageButton
            android:id="@+id/btn_return_to_start"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/return_to_start"
            android:src="@drawable/ic_skip_previous"
            android:tint="@color/text_primary" />

        <!-- Seek backward 10s -->
        <ImageButton
            android:id="@+id/btn_seek_back"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/seek_backward"
            android:src="@drawable/ic_replay_10"
            android:tint="@color/text_primary" />

        <!-- Play / Pause -->
        <ImageButton
            android:id="@+id/btn_play_pause"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_play_button"
            android:contentDescription="@string/play"
            android:src="@drawable/ic_play"
            android:tint="@color/on_accent" />

        <!-- Seek forward 10s -->
        <ImageButton
            android:id="@+id/btn_seek_forward"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/seek_forward"
            android:src="@drawable/ic_forward_10"
            android:tint="@color/text_primary" />

    </LinearLayout>

    <!-- Loop controls -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/loop_section_label"
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        android:layout_marginBottom="4dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="16dp">

        <!-- Set loop start (S key) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_set_loop_start"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/set_loop_start"
            android:textSize="12sp"
            app:strokeColor="@color/loop_tick" />

        <View android:layout_width="8dp" android:layout_height="1dp" />

        <!-- Set loop end (E key) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_set_loop_end"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/set_loop_end"
            android:textSize="12sp"
            app:strokeColor="@color/loop_tick" />

        <View android:layout_width="8dp" android:layout_height="1dp" />

        <!-- Toggle loop (L key) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_toggle_loop"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/toggle_loop"
            android:textSize="12sp"
            app:backgroundTint="@color/loop_button_inactive"
            app:icon="@drawable/ic_loop"
            app:iconGravity="start" />

    </LinearLayout>

    <!-- Speed controls -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/speed_label"
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        android:layout_marginBottom="4dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Speed down (- key) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_speed_down"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:minWidth="0dp"
            android:insetLeft="0dp"
            android:insetRight="0dp"
            android:text="@string/minus"
            android:textSize="20sp"
            app:strokeColor="@color/accent" />

        <!-- Speed display -->
        <TextView
            android:id="@+id/tv_speed"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:text="100%"
            android:textColor="@color/text_primary"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- Speed up (+ key) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_speed_up"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:minWidth="0dp"
            android:insetLeft="0dp"
            android:insetRight="0dp"
            android:text="@string/plus"
            android:textSize="20sp"
            app:strokeColor="@color/accent" />

    </LinearLayout>

    <!-- Keyboard shortcuts hint -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/keyboard_hints"
        android:textColor="@color/text_secondary"
        android:textSize="11sp"
        android:gravity="center" />

</LinearLayout>
